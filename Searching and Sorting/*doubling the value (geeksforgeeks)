//find better sol using binary search 

class Solution
{
    public:
    long long solve(int n, long long a[], long long b)
    {
        //code here.
        
long long start = b;
sort(a, a + n);
for (int i = 0; i < n; i++) {
if (a[i] == start) {
start = start * 2;
}
}
return start;

    }  
};
